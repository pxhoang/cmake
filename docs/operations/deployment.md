# Deployment

`gateway_service` can be installed as a systemd service.

Runtime config is provided through:

```text
/etc/gateway_service/gateway_service.env
```

Generated package output must be created under:

```text
.img/package/gateway_service/
```
