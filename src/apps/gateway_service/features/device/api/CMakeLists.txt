project_add_library(gateway_device_api
    SOURCES
        device_api.cpp
    PUBLIC_INCLUDE_DIRS
        ${CMAKE_CURRENT_SOURCE_DIR}
    PUBLIC_DEPENDENCIES
        project::gateway_device_application
        project::protocol
)
